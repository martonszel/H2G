import { squareAndSum, sumAndSquare } from './square-negatives-and-sum'

describe('square-negatives-and-sum', () => {

    describe('squareAndSum', () => {
        it('should pass', () => {
            expect(squareAndSum()).toBeDefined()
        })
    })

    describe('sumAndSquare', () => {
        it('should pass', () => {
            expect(sumAndSquare()).toBeDefined()
        })
    })
})